# 工程骨架与开发上下文

| 项目 | 内容 |
|------|------|
| 文档编号 | DSNTP-CTX-001 |
| 文档名称 | 工程骨架与开发上下文 |
| 版本 | **V2.2** |
| 日期 | 2026-08-09 |
| 需求基线 | **DSNTP-DRS-002 V2.0**《开发需求说明书（合并版）》 |
| 协议定稿 | DRS-002 §7 / §12；Magic=`0x5453594E`（"TSYN"），FAULT=`0x06`，Signature=`64B` |
| 用途 | 固定仓库骨架、模块边界、红线与续开发上下文，避免会话丢失后偏离需求 |

### 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-08-06 | 初版：基线 EDRS-001 V1.1，主工程指向 `dtc-c` |
| V2.0 | 2026-08-09 | 对齐 DRS-002；主开发骨架改为 `开发工程/dsntp-*`；`dtc-c` 降为参考实现 |
| V2.1 | 2026-08-09 | 正式落盘权威稿；对齐合并版路径；补全 DR-003 / AC 映射与缺口表 |
| V2.1a | 2026-08-09 | 交叉引用 C 语言专用骨架文档 DSNTP-CTX-C-001 |
| **V2.2** | **2026-08-09** | **系统级工程骨架落盘**：`api/`、`deploy/`、`docs/`、`dsntp-wide/`；ctl/ntp 可运行占位；SYS-001 |

### 与旧文档关系

| 文档 | 状态 |
|------|------|
| DSNTP-DRS-002 V2.0（合并版） | **唯一需求基线** |
| EDRS-001 / DRS-001 | 已并入 DRS-002，仅作历史参考 |
| 本 CTX V1.0 / V2.0 | 被本 V2.1 替代（V2.0 过程文档草稿并入） |
| **DSNTP-CTX-C-001**《C 语言工程骨架与开发上下文》 | **C11 Agent（`dsntp-agent`）续开发专用** |
| **DSNTP-SYS-001**《系统级工程骨架说明》 | **L1～L4 + api/deploy 总览** |

---

## 1. 文档关系与入口

```text
专利/算法源材料（S1～S6） + 实施方案/协议（S7～S8）
        │
        ▼
开发需求说明书（合并版）  DSNTP-DRS-002 V2.0   ← 唯一基线
        │
        ├─► 本文档（工程骨架与开发上下文 V2.2）
        │         │
        │         ├─► SYS-001 系统级工程骨架说明
        │         ▼
        │    开发工程/{api,deploy,docs,dsntp-agent,dsntp-ctl,dsntp-ntp,dsntp-wide}
        │
        └─► dtc-c（成熟参考实现，可对照移植，不强制为主开发树）
```

| 路径 | 说明 |
|------|------|
| `cproj/物理隔离网络分布式时间共识同步系统-开发需求说明书-合并版.md` | **DRS-002 权威基线** |
| `cproj/工程骨架与开发上下文.md` | **本文档（权威正式稿）** |
| `cproj/工程骨架与开发上下文-C语言.md` | **C11 Agent 专用骨架（CTX-C-001）** |
| `cproj/系统级工程骨架.md` / `开发工程/docs/系统级工程骨架.md` | **SYS-001** |
| `基于AI的时间同步系统设计与实现/过程文档/工程骨架与开发上下文.md` | 同步副本（须与权威稿一致） |
| `基于AI的时间同步系统设计与实现/开发工程/` | **系统级官方开发骨架根** |
| `D:\dsntp\dtc-c\` | 参考实现（C11，功能较完整） |
| `开发工程/api/agent.proto`（副本 `D:\dsntp\api\`） | IF-CTL 消息契约 |

会话续开发入口：先读 DRS-002 相关章节 + 本 CTX §6～§8，再改代码。  
**编写 / 联调 C Agent 时**：以 [工程骨架与开发上下文-C语言.md](工程骨架与开发上下文-C语言.md)（CTX-C-001）为准（头文件契约、线程模型、`agent.c` 填空顺序）。

---

## 2. 系统骨架总览（对齐 DRS-002 §4）

### 2.1 逻辑分层

```text
┌─────────────────────────────────────────────────────────────┐
│  管控层  dsntp-ctl（占位）  gRPC:50051 / HTTP:8080            │
│  配置·密钥·汇聚·告警·审计·控制台                               │
└──────────────▲──────────────────────────┬───────────────────┘
               │ 管控面（允许秒级延迟）      │ 配置/指令下发
┌──────────────┼──────────────────────────┼───────────────────┐
│              │  精同步层  dsntp-agent     │                   │
│  Agent◄─UDP:47500 TSYN─►Agent …          │                   │
│              │  get_synced_time / UDS     │                   │
└──────────────┼───────────────────────────┘                   │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  时间出口层  dsntp-ntp（占位） chrony + time-shim             │
└───────────────────────────────┬─────────────────────────────┘
                                ▼ UDP:123
┌─────────────────────────────────────────────────────────────┐
│  宽同步层  交换机 / 路由器 / 防火墙 / 摄像机（NTP/SNTP）        │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 管控面 / 数据面分离（红线）

| 平面 | 协议 | 影响授时精度 | 实现位置 |
|------|------|--------------|----------|
| 数据面 | UDP 47500 / TSYN | **是** | `dsntp-agent` 热路径（参考：`dtc-c`） |
| 管控面 | gRPC/HTTPS | **否** | `dsntp-ctl` + Agent Reporter；Server 宕机不得停共识 |

**FR-ARCH-002/003/004**：Server 不参与、不改写 \(T_c\)；Agent 本地缓存配置可离线共识；Reporter **禁止**在 UDP 热路径阻塞调用管控 I/O。

### 2.3 仓库级工程骨架（系统级布局，V2.2 已落盘）

```text
D:\dsntp\
├── cproj\                   # DRS / CTX / SYS 权威稿
├── 基于AI的时间同步系统设计与实现\
│   ├── 过程文档\            # CTX 同步副本
│   └── 开发工程\            # ★ 系统级官方骨架根
│       ├── Makefile
│       ├── api\             # agent.proto + openapi-v1.yaml
│       ├── deploy\          # topology / systemd / compose
│       ├── docs\            # 系统架构 · 接口矩阵 · SYS-001
│       ├── dsntp-agent\     # L2 M1～M3（C11）
│       ├── dsntp-ctl\       # L1 M4（Go HTTP 骨架）
│       ├── dsntp-ntp\       # L3 M5（chrony + shim）
│       └── dsntp-wide\      # L4 宽同步模板
├── dtc-c\                   # 参考实现（只读对照）
└── api\                     # IF-CTL 契约副本
```

> **原则**：精同步精度路径只在 Agent；管控与 NTP / 宽同步为旁路，可独立发版。新开发优先落在 `开发工程/`；需要完整算法细节时对照 `dtc-c`。

---

## 3. Agent 工程骨架（`开发工程/dsntp-agent`）

### 3.1 目录树

```text
dsntp-agent/
├── README.md
├── CMakeLists.txt / Makefile
├── include/dsntp/
│   ├── types.h / protocol.h / clock.h / fsm.h
│   ├── measure.h / consensus.h / recover.h
│   ├── crypto.h / net.h / config.h / api_local.h / agent.h
├── src/{protocol,clock,fsm,measure,consensus,recover,crypto,net,config,api,agent}/
├── tests/test_smoke.c
├── deploy/{agent.example.conf,dsntp-agent.service}
├── scripts/gen_keys.sh
└── docs/{模块映射.md,状态机报文对照.md}
```

### 3.2 模块 ↔ DRS-002 需求映射

| 模块 | DRS 需求 | 骨架进度 | 备注 |
|------|----------|----------|------|
| protocol | §7 IF-TSYN、OC-01～03 | Header/Packet 可用 | Magic=TSYN；FAULT=**0x06**；Sig=**64B** |
| clock | FR-CLK、FR-SYN-002 | 单调钟 + 合成公式 | `CLOCK_MONOTONIC` → ns |
| fsm | §6 FR-FSM | 合法转移；PeerFault 不改本机态 | FR-FSM-002/004 |
| measure | FR-MEAS、FR-SYN-001 | RTT/θ̂ + 回归 | CollectTimeout=22ms |
| consensus | FR-CNS | 中位数；&lt;2f+1 不更新 Tc | |
| recover | FR-FLT | 投票/静默/ACK API | agent 联调待填 |
| crypto | FR-SEC | **占位** | 对接 OpenSSL 为 M2 |
| net | UDP 47500 | bind/send/recv | 建议 `SO_PRIORITY=7` |
| config | DR-003 | key=value | 见 §3.5 |
| api | IF-APP | UDS 骨架 | `/var/run/time-agent.sock` |
| agent | §5.10 / §6.4 | 状态机主循环 + **TODO 填空** | Collecting/Consensus 待接报文 |
| reporter | FR-CTL | **未建** | M4；独立线程，无锁快照 |
| ntp-shim | FR-NTP | **未建于 agent** | M5；角色 `gateway` 可预留 |

详细源码对照见 `开发工程/dsntp-agent/docs/模块映射.md`。

### 3.3 内部数据流

```text
timer tick
    │
    ▼
Collecting ──TIME_REQ/RESP──► measure(RTT, θ̂)
    │
    ▼
Consensus ──median──► Tc（不足 2f+1 则保持旧 Tc）
    │                    + CONSENSUS_ACK ≥2f+1
    ▼
Estimating ──regress──► α̂, β̂
    │
    ▼
Running ──► clock.synced_ns ──► UDS (IF-APP)
                │
                └──（M4）Reporter 快照 ──管控面──► dsntp-ctl
```

### 3.4 构建速查

```bash
cd "基于AI的时间同步系统设计与实现/开发工程/dsntp-agent"
cmake -S . -B build && cmake --build build
ctest --test-dir build --output-on-failure
./build/dsntp-agent -c deploy/agent.example.conf
```

参考实现：`D:\dsntp\dtc-c`（CMake + OpenSSL 更完整）。

### 3.5 开局配置项（DR-003）

| 配置项 | 说明 | 热更新 |
|--------|------|--------|
| NodeID | 本机节点 ID（uint16） | 否 |
| 邻居列表 | Peer 地址与 NodeID | 否（滚动重启） |
| n / f | 节点总数 / 容错数，须 \(n\geq 3f+1\) | 否（滚动） |
| T | 同步周期（默认 1s） | 是（下轮生效） |
| L | 滑动窗口长度（默认 10） | 是 |
| Dmax | 单向时延上界（默认 10ms） | 是 |
| 数据面端口 | UDP 47500（全网一致） | 否 |
| 角色 | `consensus_only` / `gateway` | 否 |
| 密钥 / 白名单 | ECDSA-P256 私钥与公钥表 | 否（滚动） |
| server_endpoint | 管控面地址（可空） | 是 |
| 故障阈值 / 上报周期 | 连续无响应轮数、Heartbeat/Metrics 周期 | 是 |

默认工程基线：\(n=5,\ f=1\)；CollectTimeout=\(2\times\mathrm{Dmax}+\delta=22\mathrm{ms}\)；协议 Version=`0x02`。

---

## 4. 状态机—报文对照（DRS §6.3，实现检查清单）

| 状态转移 / 场景 | 事件 | 报文 / 接口 | 验收要点 |
|-----------------|------|-------------|----------|
| Init→SyncWait | Start | START_SYNC 0x01 | 验签 |
| Collecting | 采样 | TIME_REQ/RESP 0x02/03 | T0～T3；超时→PeerFaultSuspect |
| Consensus | 广播确认 | RESULT/ACK 0x04/07 | ≥2f+1 ACK |
| PeerFaultConfirm | 对端故障 | FAULT_NOTIFY **0x06** | ≥f+1；检测方不进 Fault |
| Recovering | 重入 | ANNOUNCE/ACK 0x05/08 | 静默≥2；≥2f+1 ACK |
| Ext_Sync | 单向校时 | 本地 EXT_TIME_IN | 不占 P2P Type |
| Estimating / Running 闭环 | 本地 | 无网络报文 | 合成钟单调 |

**AC-18**：上表依赖通信的转移均须有编解码与联调用例。

Agent 内完整对照表：`开发工程/dsntp-agent/docs/状态机报文对照.md`。

---

## 5. 管控 / NTP / 宽同步骨架（系统级）

### 5.1 `开发工程/dsntp-ctl`（M4）

```text
dsntp-ctl/
├── go.mod
├── cmd/server/main.go   # HTTP 可运行；gRPC 占位日志
├── internal/{ingest,config,alert,httpapi}/
└── api/                 # 说明：权威契约见 开发工程/api/
```

| 接口（IF-CTL） | 方向 | 骨架状态 |
|----------------|------|----------|
| HTTP `/api/v1/*` | 运维 → Server | **已接线**（health/nodes/config/events/ingest） |
| HTTPS JSON ingest | Agent → Server | **已接线** `/api/v1/ingest/report` |
| gRPC ReportStream | Agent ↔ Server | proto 已定；服务端待接 |

职责：FR-CTL / IF-CTL；**禁止**参与 P2P 共识或改写 \(T_c\)。

### 5.2 `开发工程/dsntp-ntp`（M5）

```text
dsntp-ntp/
├── chrony/chrony.conf.example
└── time-shim/{main.c,Makefile}
```

职责：FR-NTP / IF-NTP；消费合成时间对外 UDP 123；禁止外源取时。

### 5.3 `开发工程/dsntp-wide`（L4）

交换机 / 摄像机 NTP 开局模板 + collector 占位；见 `dsntp-wide/templates/`。

### 5.4 共享 `api/` / `deploy/` / `docs/`

| 路径 | 内容 |
|------|------|
| `api/agent.proto` | IF-CTL gRPC |
| `api/openapi-v1.yaml` | IF-CTL HTTP |
| `deploy/topology/poc-n5.yaml` | \(n=5,f=1\) 拓扑 |
| `docs/系统级工程骨架.md` | SYS-001 |

---

## 6. M1～M6 接续路径与当前缺口

| 阶段 | 内容 | 落点 | 出口准则（AC） | 当前缺口 |
|------|------|------|----------------|----------|
| M1 | 最小闭环 | dsntp-agent | AC-01（可读单调合成时间） | agent Collecting/Consensus 真实收发；payload 大端 |
| M2 | 安全与容错 | dsntp-agent | AC-03/04/07/17/18 | OpenSSL 签名；FAULT/RECOVER 接入主循环 |
| M3 | 完整算法 | dsntp-agent | 算法规格完整覆盖 | 多层 3/2 轮共识、周期自适应、Ext_Sync |
| M4 | 管控面 | dsntp-ctl + Reporter | AC-10/11/12 | HTTP 骨架已通；gRPC/Reporter/持久化待做 |
| M5 | NTP 异构 | dsntp-ntp + dsntp-wide | AC-13/14 | chrony 示例 + shim 源码骨架；联调待做 |
| M6 | 工程硬化 | deploy/ + 验收 | AC-05/06/08 | systemd/拓扑已占位；RT、72h、70% 流量待测 |

建议编码顺序（Agent）：payload 编解码 → Collecting 收发 → Consensus ACK → crypto → recover 接线 → Ext_Sync / 自适应。

### 6.1 里程碑 ↔ 验收条目速查

| 里程碑 | 主要 AC |
|--------|---------|
| M1 | AC-01、AC-02、AC-15、AC-16 |
| M2 | AC-03、AC-04、AC-07、AC-17、AC-18 |
| M3 | AC-09（若交付 Ext_Sync） |
| M4 | AC-10、AC-11、AC-12 |
| M5 | AC-13、AC-14 |
| M6 | AC-05、AC-06、AC-08 |

完整验收表见 DRS-002 §13。

---

## 7. 协议与红线速查（DRS §7 / §12）

| 项 | 定稿 |
|----|------|
| Magic | `0x5453594E`（"TSYN"） |
| Version | `0x02` |
| 结构 | Header(16B) + Payload + Signature(**64B**) |
| FAULT_NOTIFY | Type=**0x06**（禁止 0x03） |
| 强制验签 | START_SYNC、CONSENSUS_RESULT、FAULT_NOTIFY、RECOVER_ANNOUNCE |
| PeerFault | **不**导致本机进入 Fault |
| 样本 &lt; 2f+1 | **不更新** \(T_c\) |
| AlphaPpm | `alpha = (AlphaPpm/1e6)×1e-6`（IF-TSYN-015） |
| Server | 不改 \(T_c\)；宕机不中断共识与 NTP |

### 7.1 OC 定稿摘要（DRS §12）

| 编号 | 工程定稿 |
|------|----------|
| OC-01 | Signature = **64B**（r‖s） |
| OC-02 | FAULT_NOTIFY Type = **0x06** |
| OC-03 | 统一 **16B Header** |
| OC-04 | PeerFault\* / LocalFaultDetect 分离 |
| OC-05 | 全类型带 Signature；关键强制验签 |
| OC-06 | `gateway` 角色可消费合成时间对外 NTP，禁止外源取时 |
| OC-07 | 多层共识映射 TIME_* / RESULT / ACK |
| OC-08 | Ext_Sync 用本地 EXT_TIME_IN，不占 P2P Type |

OC 全文见 DRS-002 §12。

---

## 8. 与 `dtc-c` 的关系

| 工程 | 角色 |
|------|------|
| `开发工程/dsntp-agent` | **主开发骨架**（按 DRS-002 命名与分期填空） |
| `dtc-c` | **参考实现**（协议/FSM/测量/恢复更完整，可移植） |

移植时保持线格式与红线一致；命名空间 `dsntp_*` / `dtc_*` 可并存，勿混链同一进程两套状态机。

---

## 9. 文档维护

- 需求变更只改 **DRS-002（合并版）**，再同步更新本 CTX 的映射表、缺口与红线。
- 骨架目录变更须更新 §2.3 / §3 / §5，并同步 **SYS-001** 与 `开发工程/README.md`。
- **权威稿**在 `cproj/工程骨架与开发上下文.md`；变更后须同步到 `过程文档/` 与仓库根副本。
- 会话续开发时：先读 DRS-002 相关章节 + 本 CTX §6～§7，再改代码。

---

**文档结束（DSNTP-CTX-001 V2.1）**
