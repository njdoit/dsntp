# 交换机 / 路由器 NTP 开局模板（示例）
# 将 <GW_PRIMARY> / <GW_STANDBY> 替换为 dsntp-ntp 主备地址。
# 禁止配置公网 NTP。

ntp server <GW_PRIMARY> prefer
ntp server <GW_STANDBY>
ntp source interface <MGMT_IF>
# poll / interval 按厂商 CLI 调整到 300～600s

# 验收：show ntp status / 对时偏差告警阈值见 NFR-PERF-007
